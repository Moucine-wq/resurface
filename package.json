{
  "name": "resurface",
  "version": "4.1.0-beta",
  "description": "Resurface installable beta app — timezone-aware reminders, localized currency preferences, detailed categories and recurrence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/email.js && node --check public/app.js && node --check public/service-worker.js",
    "test": "node tests/smoke.js"
  },
  "engines": { "node": "22" },
  "dependencies": {}
}
