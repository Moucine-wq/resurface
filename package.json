{
  "name": "resurface",
  "version": "3.3.0",
  "description": "Resurface SaaS/PWA — interface v3 avec Web Push VAPID",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/email.js && node --check lib/stripe.js && node --check public/app.js && node --check public/service-worker.js",
    "test": "node tests/migration.js && node tests/smoke.js",
    "generate:vapid": "node scripts/generate-vapid.js"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
